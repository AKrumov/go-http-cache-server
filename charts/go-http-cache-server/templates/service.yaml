apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-http-cache-server.fullname" . }}
  labels:
    {{- include "go-http-cache-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-http-cache-server.selectorLabels" . | nindent 4 }}
