apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-gradle-cache.fullname" . }}
  labels:
    {{- include "go-gradle-cache.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-gradle-cache.selectorLabels" . | nindent 4 }}
